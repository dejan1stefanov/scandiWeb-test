1. Setting database parameters in database/DB.php.
2. Every front-end file like index.php, js, css are in public folder.
3. Models containes the classes files.
4. Execute file contains the backend files and API files.
5. If you want to make new category, you just add it in the categories table in database, make new category class, then follow the product class demands and at the end you include it in execute files.